# OTA App 总结

本工程是 **运行在 slot R (0x08020000) 的 app**，配合 bootloader 完成升级闭环。
目标芯片 STM32F412ZG（1MB Flash / 256KB RAM）。

## 一、Flash 分区

| 区域 | 地址 | 扇区 | 大小 | 说明 |
|---|---|---|---|---|
| bootloader | 0x08000000 | S0~S3 | 64K | 另一个工程 |
| config | 0x08010000 | S4 | 64K | OTA 配置 `ota_app_config_t` |
| 运行槽 R | 0x08020000 | S5~S6 | 256K | **本 app 链接地址** |
| 新固件槽 N | 0x08060000 | S7~S8 | 256K | 收到的新固件先写这里 |
| 备份槽 K | 0x080A0000 | S9~S10 | 256K | 旧 app 备份，bootloader 回滚用 |
| 备用 | 0x080E0000 | S11 | 128K | 预留 |

app 链接地址必须是 R(0x08020000)、大小 ≤256K；`main` 开头把 `SCB->VTOR` 指到 R。

## 二、文件

| 文件 | 内容 |
|---|---|
| `OTA/Inc/ota_layout.h` | flash/RAM 分区、各槽地址 |
| `OTA/Inc/ota_config.h` + `Src/ota_config.c` | config 结构/枚举 + 读写（与 bootloader 共用） |
| `OTA/Inc/ota_crc32.h` + `Src/ota_crc32.c` | CRC-32（与 bootloader 共用） |
| `OTA/Inc/ota_client.h` + `Src/ota_client.c` | OTA 客户端（与传输无关） |
| `Core/Src/main.c` | app main + 串口传输层 |

## 三、app 做的两件事

**1. 启动自检转正**（`main` 开头）
- `ota_client_boot_check(selftest)`：
  - `config.state != ready` → 正常启动，什么都不做
  - `state == ready`（刚被 bootloader 装上）→ 跑 selftest，通过则写 `state=valid, cnt=0`
  - selftest 失败 → 什么都不写，bootloader 每次启动 `cnt++`，超过 3 次自动回滚到 K

**2. 运行中收新固件**（主循环）
- `ota_client_run(&io)` 跑完整流程，`io` 是传输层接口：
  - 等包头（`magic('AOLD') | size | crc32 | version`）
  - 擦 N 槽 → 回 `READY`
  - 收数据写 N 槽（分块）
  - 校验 N 槽 CRC → 写 config `n_size/n_crc/state=new_app` → 回 `OK` → 复位
- 复位后 bootloader 接管：备份 R→K、装 N→R、置 ready、跳转，回到第 1 步。

## 四、传输层抽象

`ota_client` 不认识串口/网口，只依赖：

```c
typedef struct {
    int (*recv)(uint8_t *buf, uint32_t len, uint32_t timeout_ms);  // 收满len字节
    int (*send)(const uint8_t *buf, uint32_t len);                 // 发数据(ACK等)
} ota_io_t;
```

`main.c` 里实现 `uart_recv/uart_send` 并构造 `ota_io_t`；换网口只需再写一份 recv/send。

**注意事项**：擦 N 槽要几秒，这期间不收数据，所以协议设计了 `READY` 应答——上位机必须
等到 `READY` 再发固件数据，否则会丢。应答还有 `OK`、`ERR`。

## 五、构建

```sh
cmake --preset Debug
cmake --build build/Debug
# 产物 build/Debug/ota.elf, 链接在 0x08020000
```

## 六、上位机发包示例

```python
import zlib, struct, serial
fw = open("app.bin", "rb").read()          # objcopy -O binary app.elf app.bin
crc = zlib.crc32(fw) & 0xFFFFFFFF
ser = serial.Serial("COM7", 115200, timeout=10)
ser.write(struct.pack("<IIII", 0x444C4F41, len(fw), crc, 0x00010000))  # 包头
assert ser.read(6) == b"READY\n"           # 等擦除完成
ser.write(fw)                              # 发数据
# 等 OK, 然后设备复位安装
```

CRC 用标准 zlib，与 `ota_crc32.c` 一致。

## 七、遗留 / TODO

- **首次安装**：空片 bootloader 是 `state=fail`，需用编程器把 app 烧到 R，并把
  `r_size/r_crc/state=valid` 写进 config，才能跳起来。
- **fail 无出路**：app 跑不了时只能 SWD 救。
- **看门狗**：擦写/拷贝期间未喂狗。
