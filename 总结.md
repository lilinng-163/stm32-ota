# OTA Bootloader 总结

目标芯片 STM32F412ZG（1MB Flash / 256KB RAM）。本工程是 bootloader，负责上电校验 app、升级、失败回滚。

## 一、Flash 分区（1MB）

| 区域 | 地址 | 扇区 | 大小 | 说明 |
|---|---|---|---|---|
| bootloader | 0x08000000 | S0~S3 | 64K | 本工程 |
| config | 0x08010000 | S4 | 64K | OTA 配置 `ota_app_config_t` |
| 运行槽 R | 0x08020000 | S5~S6 | 256K | app 固定链接地址，只从这里跑 |
| 新固件槽 N | 0x08060000 | S7~S8 | 256K | 收到的新固件先放这里 |
| 备份槽 K | 0x080A0000 | S9~S10 | 256K | 升级前旧 app 备份，用于回滚 |
| 备用 | 0x080E0000 | S11 | 128K | 预留 |

**为什么是"固定运行槽 + 备份"而不是真 A/B 切槽**：同一份 app 二进制没法在 0x08020000 和 0x08080000 两个基地址直接运行（向量表、`_sidata` 都是绝对地址），切槽需要 app 做重定位。所以采用固定运行槽 R，升级时把 N 拷到 R；升级前把旧 R 备份到 K，自检失败可用 K 回滚。代价是 app 必须 ≤256K，且每次升级有两次拷贝。

## 二、config 结构（`ota_config.h`）

```c
typedef struct {
    uint32_t header;   // magic, 命中才认为配置有效
    uint32_t state;    // ota_state
    uint32_t cnt;      // ready 状态下连续启动次数, 超限回滚
    uint32_t r_size, r_crc;   // 运行槽 R 的镜像大小/CRC
    uint32_t n_size, n_crc;   // 新固件槽 N 的镜像大小/CRC
    uint32_t k_size, k_crc;   // 备份槽 K 的镜像大小/CRC
} ota_app_config_t;
```

写入时**先写 body、最后写 header**，header 存在即代表配置完整。

## 三、状态机（`ota_boot.c` → `ota_check_config`）

| 状态 | 含义 | 动作 |
|---|---|---|
| `valid` | R 槽可用 | 校验 R（`app_is_valid` 粗看 + CRC）→ 通过则 `jump2app()`；否则 `fail` |
| `new_app` | N 槽有新固件 | 校验 N → 备份 R→K → 置 `installing` 落盘 → 复位 |
| `installing` | 已备份，正在安装 | 校验 N → 擦 R、N→R、回读校验 → 置 `ready` 落盘 → 复位 |
| `ready` | R 刚写入，等自检 | R 坏或 `cnt>=3` → 回滚 K→R、置 `valid`；否则 `cnt++` 后跳转 |
| `fail` | 无可用 app | 停在 bootloader |

关键顺序（掉电安全）：
1. 校验 N 通过才动 R；
2. 备份 R→K 先提交（`installing`），再擦 R、写 R，中途掉电可重来；
3. 写入完成并回读校验后才置 `ready`。

## 四、关键函数

文件（`OTA/Inc` + `OTA/Src`）：

| 文件 | 内容 |
|---|---|
| `ota_layout.h` | flash/RAM 分区、各槽地址 |
| `ota_config.h/.c` | config 结构/枚举 + 读写 |
| `ota_crc32.h/.c` | CRC-32 |
| `ota_boot.h/.c` | 状态机 `ota_check_config` |
| `ota_jump.h/.c` | `app_is_valid` / `jump2app` |

- `crc32_flash(addr, len)`（`ota_crc32.c`）：标准 CRC-32（zlib 一致），逐字节，不用对齐。
- `verify_slot(base, size, crc)`：校验某个槽。
- `erase_slot(sector)` / `copy_slot(src, dst, size)`：擦/拷一个槽。
- `rollback_to_k(cfg)`：K→R 回滚。
- `app_is_valid()` / `jump2app()`（`ota_jump.c`）：向量表粗检 / 跳转到 R。
- `ota_check_config()`：状态机入口，`main()` 里调用。

## 五、main 流程

```
HAL_Init → SystemClock_Config → MX_GPIO/USART3/USB_Init
         → printf(bootloader start)
         → ota_check_config()   // 正常会跳app或复位, 不返回
         → while(1)             // 只在 state==fail 时走到
```

printf 通过 `__io_putchar` → USART3（PD8/PD9，115200）。

## 六、app 侧还需要做的（另一个工程）

1. app 链接地址必须是 `SLOT_R_BASE`(0x08020000)，大小 ≤256K；启动时把 `SCB->VTOR` 指到 `SLOT_R_BASE`（开中断前）。
2. 收到新固件 → 写 `SLOT_N_BASE` → 计算 CRC → 更新 config 的 `n_size/n_crc`、`state=new_app` → 复位。
3. 启动时若 `config.state == ready` → 自检 → 通过则写 `state=valid, cnt=0`；不通过则**什么都不写**，让 bootloader 计数超限后回滚。
4. app 与 bootloader 共用同一份 `crc32`（算法一致）和 `config` 结构。

## 七、遗留 / TODO

- **`fail` 状态没有出路**：停在 `fail` 后 app 跑不了、bootloader 也不接收固件，只能 SWD 救。建议加兜底接收通道。
- **首次安装**：空片第一次是 `fail` 且 `r_size=0`，编程器烧 app 后还需把 `r_size/r_crc/state=valid` 写进 config，否则跑不起来。需要出厂烧录脚本配合或加处理。
- **看门狗**：`copy_slot` 拷贝期间未喂狗。
- **未上板验证**。
- 下载区：本方案没有独立下载区，新固件直接进 N 槽。

## 八、构建

```sh
cmake --preset Debug
cmake --build build/Debug
```

产物：`build/Debug/ota.elf`（当前约 20.5K Flash / 4.4K RAM）。
